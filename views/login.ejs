<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #0a3d62;
            margin: 0;
            color: white;
        }
    
        .container {
            text-align: center;
            max-width: 450px; 
            width: 100%; 
            padding: 15px 20px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
            border-radius: 20px;
            background-color: rgba(255, 255, 255, 0.2); 
        }
    
        h2 {
            text-align: center;
            margin-bottom: 20px;
            color: rgb(255, 255, 255);
            font-size: 24px;
        }
    
        label {
            padding: 15px 20px;
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #fefefe;
        }
    
        input[type="text"], input[type="email"], input[type="password"] {
            width: 100%;
            padding: 15px 5px;
            margin-bottom: 20px;
            border: 1px solid #ced4da;
            border-radius: 25px; /* Rounded corners */
            font-size: 16px;
            color: #fff; 
            background-color: rgba(0, 0, 0, 0.5); 
        }
    
        /* Transparent placeholder */
        input::placeholder, 
        input[type="text"]::placeholder, 
        input[type="email"]::placeholder, 
        input[type="password"]::placeholder {
            color: rgba(255, 255, 255, 0.5); 
            opacity: 1; 
        }
    
        input[type="text"]:focus, 
        input[type="email"]:focus, 
        input[type="password"]:focus {
            border-color: #007BFF;
            outline: none;
        }
    
        input[type="submit"] {
            background-color: #007BFF;
            color: rgb(252, 250, 250);
            border: none;
            padding: 12px;
            border-radius: 25px; 
            cursor: pointer;
            font-size: 16px;
            width: 100%; 
        }
    
        button {
            background-color: #ff6b6b;
            border: none;
            border-radius: 25px;
            padding: 15px 20px;
            margin-top: 20px;
            width: 300px; 
            color: white;
            font-size: 1em;
            cursor: pointer;
        }
    
        
        @media (max-width: 768px) {
            .container {
                width: 90%; 
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Login </h2>
        <form action="/auth/login" method="POST">
            
            <label for="username"></label>
            <input type="text" id="username" name="username" placeholder="Masukkan Nama Anda" required>

            <label for="password"></label>
            <input type="text" id="password" name="password" placeholder="Masukkam Password Anda" required>

        <button type="submit">Login</button>  
        </form>
        <p> don't have an account <a href="/auth/register">Registrasi here</a></p>
    </div>
</body>
</html>